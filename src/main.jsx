import React from "react";
import ReactDOM from "react-dom/client";
import AutoProfit from "./AutoProfit.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AutoProfit />
  </React.StrictMode>
);
